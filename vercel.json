{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "sh -c 'if test -f ../client/package.json; then cd .. && npm install --include=dev; else npm install --include=dev; fi'",
  "buildCommand": "node scripts/vercelBuild.mjs",
  "outputDirectory": "deploy-static",
  "framework": null,
  "functions": {
    "src/index.ts": {
      "memory": 2048,
      "maxDuration": 60,
      "includeFiles": "server/dist/**"
    }
  }
}
